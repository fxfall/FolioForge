#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)
if [ "${1:-}" = "kfx-probe" ]; then
    shift
    exec python3 "$root/tools/dev/kfx_probe.py" "$@"
fi

echo "usage: tools/dev/folio-dev kfx-probe {list|validate|build|evidence|coverage}" >&2
exit 2
