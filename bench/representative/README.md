# Representative corpus manifest

Add privacy-safe local paths at run time. Do not commit book content or
copyrighted samples. The expected fields are `name`, `input`, `format`, and
`notes`; reports belong in the external validation directory.
