# Micro benchmark inputs

Use `tests/semantic-equivalence/basic/basic.txt` and the XHTML/degradation
fixtures for repeatable small-input measurements. The runner must copy or write
all generated outputs below `FOLIOFORGE_VALIDATION_ROOT`, never beside source
files.
