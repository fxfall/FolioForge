# Stress recipes

The stress set is generated at run time: large text, large HTML DOM,
image-heavy EPUB, large DOCX and FB2 resources. Generated input/output bytes
must stay under the external validation directory.
