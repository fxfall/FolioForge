# INLINE-01 — non-image render-inline control

Control for an inline visual object that is neither MathML nor a figure image.
The probe exists to make an encountered non-image render-inline structure
visible in coverage rather than silently treating it as a normal paragraph.
