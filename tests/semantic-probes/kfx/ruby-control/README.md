# RUBY-01 — ruby control

Small ruby regression probe. Ruby already has real-book evidence in the
project, so this fixture checks that the semantic probe harness can keep the
existing behavior under the new evidence order.
